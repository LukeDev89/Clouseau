@* Legacy wrapper for backward compatibility - new modular components are in Components folder *@
@using DataContext

@code {
    // This is a legacy wrapper component - the functionality has been moved to the modular ProjectTasks.razor
    // This component is kept for backward compatibility but does not provide any functionality
}